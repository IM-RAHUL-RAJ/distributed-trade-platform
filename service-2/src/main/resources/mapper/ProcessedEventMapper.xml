<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.s2.mapper.ProcessedEventMapper">

    <insert id="insertIgnore">
        INSERT INTO processed_events (event_id, event_type, order_id, payload, processed_at)
        SELECT #{eventId}, #{eventType}, #{orderId}, #{payload}, NOW()
        WHERE NOT EXISTS (
            SELECT 1 FROM processed_events e WHERE e.event_id = #{eventId}
        )
    </insert>

</mapper>