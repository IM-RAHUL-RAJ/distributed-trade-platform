<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.s2.mapper.OrderMapper">

    <select id="findById" resultType="com.trade.platform.s2.entity.OrderRow">
        SELECT id, user_id, account_id, instrument_id, symbol, side, order_type, quantity,
               requested_price, executed_price, status, reject_reason, created_at, updated_at
        FROM orders WHERE id = #{id}
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status},
            executed_price = #{executedPrice},
            reject_reason = #{rejectReason},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>