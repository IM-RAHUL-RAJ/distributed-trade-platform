<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.s2.mapper.AccountMapper">

    <select id="lockById" resultType="com.trade.platform.s2.entity.Account">
        SELECT id, user_id, cash, margin, status, created_at, updated_at
        FROM accounts WHERE id = #{id} FOR UPDATE
    </select>

    <update id="updateCash">
        UPDATE accounts SET cash = #{cash}, updated_at = NOW() WHERE id = #{id}
    </update>

</mapper>