<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.mapper.LatestEventMapper">

    <insert id="insert">
        INSERT INTO latest_events (id, event_type, order_id, payload, received_at)
        VALUES (#{id}, #{eventType}, #{orderId}, #{payload}, NOW())
    </insert>

</mapper>