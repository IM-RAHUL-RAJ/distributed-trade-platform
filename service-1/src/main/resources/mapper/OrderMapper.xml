<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.mapper.OrderMapper">

    <insert id="insert" parameterType="com.trade.platform.entity.OrderRow">
        INSERT INTO orders (id, user_id, account_id, instrument_id, symbol, side, order_type,
                            quantity, requested_price, executed_price, status, reject_reason, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{accountId}, #{instrumentId}, #{symbol}, #{side}, #{orderType},
                #{quantity}, #{requestedPrice}, #{executedPrice}, #{status}, #{rejectReason}, NOW(), NOW())
    </insert>

    <select id="findById" resultType="com.trade.platform.entity.OrderRow">
        SELECT id, user_id, account_id, instrument_id, symbol, side, order_type, quantity,
               requested_price, executed_price, status, reject_reason, created_at, updated_at
        FROM orders WHERE id = #{id}
    </select>

    <select id="findByIdAndUser" resultType="com.trade.platform.entity.OrderRow">
        SELECT id, user_id, account_id, instrument_id, symbol, side, order_type, quantity,
               requested_price, executed_price, status, reject_reason, created_at, updated_at
        FROM orders WHERE id = #{id} AND user_id = #{userId}
    </select>

    <select id="findByUser" resultType="com.trade.platform.entity.OrderRow">
        SELECT id, user_id, account_id, instrument_id, symbol, side, order_type, quantity,
               requested_price, executed_price, status, reject_reason, created_at, updated_at
        FROM orders WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findOpenByUser" resultType="com.trade.platform.entity.OrderRow">
        SELECT id, user_id, account_id, instrument_id, symbol, side, order_type, quantity,
               requested_price, executed_price, status, reject_reason, created_at, updated_at
        FROM orders WHERE user_id = #{userId} AND status = 'PENDING' ORDER BY created_at DESC
    </select>

    <update id="updateStatus">
        UPDATE orders
        SET status = #{status},
            executed_price = #{executedPrice},
            reject_reason = #{rejectReason},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

</mapper>