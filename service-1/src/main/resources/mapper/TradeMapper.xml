<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.mapper.TradeMapper">

    <select id="findByUser" resultType="com.trade.platform.entity.Trade">
        SELECT id, order_id, user_id, account_id, instrument_id, symbol, side, quantity, price, executed_at
        FROM trades WHERE user_id = #{userId} ORDER BY executed_at DESC
    </select>

</mapper>