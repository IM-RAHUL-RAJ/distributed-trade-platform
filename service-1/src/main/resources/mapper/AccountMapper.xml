<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.trade.platform.mapper.AccountMapper">

    <select id="findById" resultType="com.trade.platform.entity.Account">
        SELECT id, user_id, cash, margin, status, created_at, updated_at
        FROM accounts WHERE id = #{id}
    </select>

    <select id="findByUserId" resultType="com.trade.platform.entity.Account">
        SELECT id, user_id, cash, margin, status, created_at, updated_at
        FROM accounts WHERE user_id = #{userId}
    </select>

    <insert id="insert" parameterType="com.trade.platform.entity.Account">
        INSERT INTO accounts (id, user_id, cash, margin, status, created_at, updated_at)
        VALUES (#{id}, #{userId}, #{cash}, #{margin}, #{status}, NOW(), NOW())
    </insert>

    <update id="updateCash">
        UPDATE accounts SET cash = #{cash}, updated_at = NOW() WHERE id = #{id}
    </update>

    <insert id="insertDefaultAccount">
        INSERT INTO accounts (user_id, cash, margin, status, created_at, updated_at)
        VALUES (#{userId}, #{cash}, 0, 'ACTIVE', NOW(), NOW())
    </insert>

    <insert id="insertDefaultWatchlist">
        INSERT INTO watchlists (user_id, instrument_id, created_at)
        SELECT #{userId}, id, NOW() FROM instruments
        WHERE symbol IN
        <foreach collection="symbols" item="s" open="(" separator="," close=")">
            #{s}
        </foreach>
        ON CONFLICT (user_id, instrument_id) DO NOTHING
    </insert>

</mapper>