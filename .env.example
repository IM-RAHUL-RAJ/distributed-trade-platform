# All environment variables are optional. Uncomment and change to override defaults.

# PostgreSQL
DB_NAME=trade_platform
DB_USER=trade_app
DB_PASSWORD=trade_app_password

# Kafka
KAFKA_CLUSTER_ID=5L6g3nShT-eMCtK--X86sw

# Host ports (each service listens on 8080/80 inside its container)
POSTGRES_PORT=5432
KAFKA_PORT=9092
SERVICE1_PORT=8081
SERVICE2_PORT=8082
BFF_PORT=3000
FRONTEND_PORT=4200
MOCK_API_PORT=8080

# Auth
JWT_SECRET=dev-only-secret-change-me-0123456789abcdef
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL_SECONDS=604800

# Service 2
MARKET_DATA_POLL_INTERVAL_MS=5000